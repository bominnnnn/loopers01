server:
  port: 3000

spring:
  application:
    name: loopers-server

jwt:
  # 운영 환경에서는 반드시 32자 이상의 랜덤 문자열로 교체하세요
  secret: loopers_super_secret_key_must_be_at_least_32_characters
  expiration-days: 7

cors:
  allowed-origin: http://localhost:5173
